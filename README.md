# 📊 Polling/Voting App

A full-featured polling application built with Flask and SQLite.

## 🚀 Quick Start

```bash
# Install dependencies
pip install flask

# Run the app
python app.py

# Open browser at http://localhost:5000
